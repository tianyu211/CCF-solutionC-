#include <bits/stdc++.h>
using namespace std;

struct a{
	int a;
	int b;
};
a x[2];

int main()
{
	x[1].b = 33;
	cout<<x[1].b;
	/*int size=1024;
	char* buff = (char*)malloc(size);
	
	//read lines
	while(gets(buff) != NULL){
		printf("Read line with len: %d\n", strlen(buff));  
        printf("%s", buff);  
	}
	
	//ÊÍ·Åbuff 
	free buff; 
	
	string s = "asd dsa";
	int pos = s.find(" ");
	cout<<s.length();*/
	 
	return 0;
}
